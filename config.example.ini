[storage]
data_root = /home/dev/bili-summary-data

[processing]
audit_level = basic
transcriber_mode = auto
cost_submission_limit_cny = 1.0

# Secrets are intentionally absent. Future API keys must be supplied through
# environment variables or another ignored local secret store.
