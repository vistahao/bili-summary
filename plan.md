# 哔哩哔哩与本地学习视频自动整理工具计划

> 文档状态：需求与技术路线定稿 v1.2；阶段 4 已验收，进入阶段 5
> 更新日期：2026-08-31
> 当前阶段：阶段 1～4 已验收；阶段 5 开始处理缓存、备份迁移、极长视频与日常可靠性
> 本版依据：在 v1.1 基础上新增按文本任务选择模型与推理强度的能力；直接调用 DeepSeek API，不引入 ZCode；配置文件保存默认值，交互终端在首次模型调用前允许本次覆盖

## 1. 项目定位

本项目服务于个人课程学习。输入一个哔哩哔哩视频链接或本地 MP4 文件，工具优先取得可用字幕；没有字幕时再取得音频并转写。随后生成可核对、可继续整理、适合长期积累的中文学习资料。

主要学科是：

- 计算机
- 数学
- 电磁学
- 电子
- 以后新增的其他学习主题

第一版只在本机运行，一次处理一个公开视频或一个本地 MP4。先完成可靠的命令行流程，再根据真实使用决定 UI、Obsidian 和 PPT 功能。

## 2. 第一版交付内容

每个视频不只生成“摘要”，而是交付一套互相可追溯的学习资料。

### 2.1 固定输出文件

| 文件 | 用途 | 默认保留 |
|---|---|---|
| `字幕.srt` | 保存未经模型改写的带时间戳字幕；没有平台、外置或内嵌字幕时保存原始转写结果 | 长期保留 |
| `完整整理稿.md` | 修正断句、口头重复和明显转写问题后的全文，不随意改变讲者原意 | 长期保留 |
| `审校报告.md` | 默认记录基础审校结果；可选深度审校会额外检查全文知识风险，并附返回原视频的时间戳 | 长期保留 |
| `学习总结.md` | 提炼知识结构、关键概念、公式、例子、结论和复习问题 | 长期保留 |
| `source.json` | 记录输入类型、原链接或本地路径、BV 号/文件哈希、文本来源、处理版本、每项文本任务的后端/厂商/模型/推理档位、转写厂商、用量/费用、状态和内部任务 ID | 长期保留 |

如平台或本地原始字幕格式包含 `SRT` 无法完整保存的信息，可额外保留体积很小的 `原始字幕.json`，用于以后重新处理。它是可复现依据，不属于大体积缓存。

### 2.2 `学习总结.md` 的建议结构

1. 视频信息与文本来源。
2. 一页速览。
3. 学习目标与所需前置知识。
4. 知识结构图，以标准 Markdown 层级列表表达。
5. 分章节详细总结，每节附原视频时间点。
6. 定义、定理、公式和推导；数学内容使用 LaTeX 公式语法。
7. 案例、代码或实验步骤。
8. 易错点、限制条件和讲者强调事项。
9. 审校风险摘要，链接到 `审校报告.md`。
10. 复习问题与可执行的后续学习建议。

该结构兼顾 Typora 阅读和幕布导入。第一版坚持标准 Markdown，不依赖某个笔记软件的私有语法。

### 2.3 审校的职责边界

“再次接入模型审核全文”拆成两个不同任务和三个档位：

- **文本审校**：寻找同音词、专业名词、断句、公式口述等疑似识别错误。原字幕不能被覆盖，所有修正都能回查。
- **知识风险审校**：寻找自相矛盾、量纲异常、概念混淆或疑似事实错误，记录原句、风险原因和时间戳。

| 档位 | 行为 | 默认值 |
|---|---|---|
| `off` | 不做知识风险审校；报告注明未执行 | 否 |
| `basic` | 在整理和总结过程中顺带记录明显风险，不单独再审全文 | 是 |
| `deep` | 独立、完整地再审一次全文，覆盖更充分但消耗更多订阅额度和时间 | 否，按任务选择 |

无论选择哪一档，都会生成 `审校报告.md` 并注明审校档位和覆盖范围。模型复核不能证明知识一定正确。第一版只标记风险，由用户返回原视频确认；暂不自动联网查证每项知识。

## 3. 第一版明确不做的内容

- 不绕过登录、付费、地区、会员或版权限制。
- 不永久收藏完整视频。
- 不做批量抓取、并发爬取或远程服务。
- 本地文件第一版只正式支持 MP4；不递归扫描整个课程目录，也不批量处理文件夹。
- 不在第一版开发网页、桌面应用、账号系统或 Obsidian 插件。
- 不在第一版分析视频画面或提取关键帧。画面理解列为后续可选扩展。
- 不在第一版直接生成 PPT；先让学习资料具备稳定结构，再开发独立的 PPT 转换器。

## 4. 自动处理流程 v1.2

1. 接收哔哩哔哩链接或本地 MP4，可选指定分P、主题和总结详细度。
2. 链接输入解析 BV 号、分P、标题、作者和发布时间；本地输入规范化 Windows/WSL 路径，检查文件并记录名称、大小、修改时间和 SHA-256。
3. 根据规则生成可读的主题目录和视频目录名。
4. 按以下顺序取得文本：
   1. 链接输入优先使用 UP 主或平台提供的人工字幕，再尝试平台可访问的 AI 字幕；
   2. 本地 MP4 优先使用同目录同名的外置 SRT，再检查并提取内嵌字幕轨；
   3. 没有可用字幕时，从平台媒体或本地 MP4 取得音频并进入同一转写分支；默认后端由阶段 4 的国内 API 小样本实测决定，本地 CPU 转写作为隐私和故障兜底；
   4. 输入不可访问、格式不支持或转写不获授权时停止，保留任务状态并说明原因。
5. 把来源文本统一为带时间戳的内部格式，并输出 `字幕.srt`。
6. 按“整理”任务所选模型配置对全文做文本审校，输出 `完整整理稿.md`；疑似修正保留来源时间点。
7. 按 `off/basic/deep` 档位和相应模型配置生成 `审校报告.md`；只有 `deep` 会独立再审全文。
8. 按语义和时间切段总结，再由“总结”任务所选模型配置合并成 `学习总结.md`。
9. 检查重要观点能否映射回字幕；没有依据时标记“不确定”。
10. 保存每次调用的任务、执行后端、实际厂商、模型、推理档位、耗时、token、调用次数，以及适用时的 API 费用估算。
11. 成功或失败都执行可预测的缓存管理；临时音频保留 5 天后才允许清理。

```text
哔哩哔哩链接 → 平台元数据 → 平台字幕 ───────────┐
本地 MP4 → 文件元数据 → 外置/内嵌字幕 ─────────┤
                   没有可用字幕 → 音频 → 转写后端 ─┘
                                                ↓
                              统一的带时间戳文本 → 字幕.srt
   ↓
文本审校 → 完整整理稿.md
   ↓
知识风险审校 → 审校报告.md
   ↓
分段总结与回溯检查 → 学习总结.md
```

## 5. 最小架构

第一版采用本机命令行。原因是依赖少、每一步容易观察，也能作为未来 UI、批量队列或 PPT 工具的稳定底层入口。

内部按职责拆分：

- `输入解析`：识别普通链接、短链接、BV 号、分P、本地 MP4、Windows 路径和 WSL 路径。
- `平台访问`：读取元数据、平台字幕和访问状态。
- `本地媒体访问`：检查文件身份、外置/内嵌字幕和媒体轨，不修改原文件。
- `音频处理`：仅在无字幕时从平台媒体或本地 MP4 取得、压缩、切分临时音频。
- `转写适配器`：对国内在线转写、本地转写或未来其他 API 提供统一接口；厂商返回值先转换为同一种带时间戳格式。
- `文本处理`：字幕转换、断句、切段、去重和时间戳映射。
- `文本审校`：生成完整整理稿和修订记录。
- `知识审校`：支持关闭、基础和深度档位；生成风险项，不静默修改讲者观点。
- `文本模型适配器`：用统一接口封装 Codex CLI、DeepSeek HTTP API，以及通过验证后的其他本地或在线执行器；适配器只返回结构化结果，不直接写成果文件。
- `文本模型路由`：为整理、总结、基础审校和深度审校分别选择命名配置；默认保持当前 Codex 行为，用户可按任务切换模型和推理强度。
- `总结`：通过模型适配器生成固定结构的学习总结。
- `主题归档`：自动建议主题，允许用户显式覆盖。
- `存储与任务状态`：管理结果、缓存、费用和失败恢复。
- `命令行入口`：展示进度、预计资源/费用、错误和最终文件位置。

这些名称表示职责，第一版不必为每项创建一个独立包或复杂框架。

### 5.1 文本模型解耦边界

核心流程只依赖一个小型 `TextBackend` 契约：后端声明自身标识、实际厂商、模型、推理档位和能力，并接受提示词与输出结构，返回结构化内容、用量、耗时和标准化错误。主程序继续独占时间戳验证、Schema 验证、缓存、合并和最终文件写入，因此更换模型不会改变数据管理边界。

文本处理拆成四个可路由的逻辑任务：

| 任务 | 产物 | 可独立选择配置的原因 |
|---|---|---|
| `organize` | 完整整理稿片段 | 侧重忠实、断句和术语修正 |
| `summary` | 分段笔记与最终学习总结 | 侧重结构、压缩和教学表达 |
| `basic_audit` | 随流程产生的明显风险项 | 侧重低误报和时间戳依据 |
| `deep_audit` | 独立全文风险审校 | 需要更完整覆盖和较高推理强度 |

阶段 3 当前把整理、分段总结笔记和 Basic 风险放在同一次模型调用中。阶段 3.1 会拆开提示词和输出结构边界，才能真正做到逐任务选模型；只有配置完全相同时才允许以后评估是否合并调用，不能为节省调用而破坏路由语义。

2026-08-31 根据知识讲座与刷题课的真实成果复核，长流程增加 `lecture` 和 `practice` 两种内容模式，并把逐片依赖调整为“Basic 审校 → 整理 → 分片总结”。最终总结只读取已经过滤的整理内容及 Basic 风险，不能从原始字幕恢复歌曲、候场或无关闲聊。`practice` 保留题意、答案、推理、选项辨析、方法和易错点；连续无关区间仅保留时间范围。高置信字幕错误可用于整理，知识或逻辑风险只约束表述并留在报告中；Deep 审校继续独立执行。该调整不增加调用次数，也不增加长期成果文件。

文本后端只保留两条清晰路径：当前已验证的 `CodexExecBackend`，以及阶段 3.1 实现的 `DeepSeekHttpBackend`。DeepSeek 使用官方 HTTP API 直接接入，不增加桌面应用或中间代理层。

缓存指纹必须包含任务类型、后端、实际厂商、模型、推理档位、提示词版本、Schema 版本和输入哈希。这样更换 DeepSeek 模型或调整推理强度后不会误复用旧结果。

### 5.2 本地 MP4 输入边界

- 命令行在 WSL 中运行，规范路径使用 `/mnt/d/...`；同时接受带盘符的 Windows 绝对路径并先规范化，避免要求用户手工改写每个路径。
- 第一版只处理单个 `.mp4` 文件，扩展名大小写不敏感；其他容器以后根据真实样例增加。
- 先寻找同目录同名的 `.srt`，再用 `ffprobe` 检查 MP4 的视频、音频和内嵌字幕轨；只有没有可用字幕时才用 `ffmpeg` 提取音频。
- FFmpeg 起源于 2000 年启动的开源多媒体项目，`ffmpeg` 负责转换媒体，配套的 `ffprobe` 负责只读探测轨道。Ubuntu 的一个 `ffmpeg` 软件包同时提供二者；不再安装功能重叠的 Python 视频库。
- 自动处理不需要 Windows 或 WSL 视频播放器。播放器只服务人工观看，不属于运行依赖；第一版也不开发播放器集成。
- 默认直接顺序读取 `/mnt/d/...` 文件。只有实测跨文件系统读取明显成为瓶颈时，才复制到 WSL 临时缓存；副本遵守 5 天规则并明确标记为可删除。
- 原 MP4 默认不复制到成果目录、不改名、不删除。文件移动或删除后，已有成果仍可阅读，但重新提取、验证或复现需要用户找回原文件。

## 6. 文件命名与主题归档

### 6.1 可读文件名

用户可见的目录和文件不使用 BV 号或无意义数字命名。

```text
/home/dev/bili-summary-data/
└── 计算机/                         # 学科
    └── 生成式软件工程/             # 可选的课程或系列
        └── 视频标题或分P标题/
            ├── 字幕.srt
            ├── 完整整理稿.md
            ├── 审校报告.md
            ├── 学习总结.md
            └── source.json
```

成果默认放在 WSL 内、源代码目录之外。这样升级或删除代码仓库时，不容易误伤长期学习资料。实施时需要为该独立目录取得一次明确写入授权。

对于“生成式软件工程”这类长课程，用户可以在命令中指定学科和课程。文档统一采用下面的规范写法：

```bash
bili-summary run "https://www.bilibili.com/video/BV1xxxxxxxxx" --subject "计算机" --course "生成式软件工程"
```

本地 MP4 使用独立子命令，避免程序把文件路径误判为链接。该样例在 WSL 中的规范写法是：

```bash
bili-summary run-file "/mnt/d/Downloads/04_公考事业编/视频课程/行测/20250828 片段刷题4.mp4" --subject "公考事业编" --course "行测"
```

也接受直接粘贴带盘符的 Windows 路径：

```bash
bili-summary run-file "D:\Downloads\04_公考事业编\视频课程\行测\20250828 片段刷题4.mp4" --subject "公考事业编" --course "行测"
```

空格和符号的含义如下：

- `bili-summary`、`run`/`run-file`、链接/文件路径、`--subject`、学科值、`--course`、课程值之间各有一个半角空格。Bash 把这些空格当作参数边界；连续多个半角空格效果相同，但文档只写一个。
- 引号包住的是一个完整参数。中文值没有空格时引号可以省略，但文档始终保留引号，便于以后使用“生成式软件工程 第一课”这类含空格名称。
- `<视频链接>` 只是在说明文档中表示“请替换这里”，实际执行时不输入尖括号，直接粘贴真实链接。链接可能含 `&` 等 shell 特殊字符，因此也统一加引号。

`--course` 可省略；省略时视频直接放在学科目录下。命令行参数只决定归档位置，不改变视频内容的模型判断。

命名规则：

- 链接输入优先使用清洗后的视频标题或分P标题；本地输入优先使用不含 `.mp4` 的文件名。
- Windows 文件名不允许的字符会替换为可读符号。
- 同名冲突时优先增加 UP 主名称或分P标题，不直接暴露 BV 号。
- 链接输入的 BV 号、本地输入的 SHA-256 和内部任务 ID 只写入 `source.json`，用于去重和恢复。
- 支持用户通过 `--subject`、`--course` 和 `--title` 覆盖学科、课程和显示名称。

### 6.2 主题规则

- 第一版内置 `计算机`、`数学`、`电磁学`、`电子`、`未分类` 五个学科。
- “课程”是学科下的可选第二级目录，不预设名称，由用户传参或采用模型建议。
- 模型可以提出学科和课程建议，但用户参数优先。
- 一个视频只有一个主目录，其他交叉主题记录为标签，避免重复保存全文。
- 未来 Obsidian 接口使用这些主题和标签，不需要搬动已有正文。

## 7. 数据、空间和迁移

### 7.1 数据分级

| 类别 | 示例 | 默认策略 | 备份建议 |
|---|---|---|---|
| 源代码 | 程序、测试、文档 | Git 管理 | 必须备份到代码仓库 |
| 项目关键配置 | 配置模板、提示词、输出结构版本 | 随源代码管理，不含秘密 | 必须随代码备份 |
| 用户运行配置 | `config.ini` 中的数据目录、分片阈值、默认文本路由和命名预设 | 本机使用、Git 忽略且不含密钥 | 必须单独备份或导出；迁移后检查路径和模型 ID |
| 私密配置 | DeepSeek/转写 API 密钥、可选 Cookie | 本机环境变量或权限为 `600` 且被 Git 忽略的配置 | 单独放入密码管理器；禁止进入 Git |
| 用户成果 | 字幕、完整整理稿、审校报告、学习总结、元数据 | 默认长期保留，不进入 Git | 必须备份 |
| 本地原视频 | Windows D 盘或其他用户目录中的 MP4 | 原地读取，不复制进成果库，不由工具清理 | 若需以后重跑则由用户在原视频库中单独备份 |
| 临时音频 | 无字幕时取得的压缩音频和切片 | 统一保留 5 天，之后才可清理 | 不备份 |
| 云端临时对象 | 为在线转写短期上传的音频 | 取回结果并核验后立即删除；失败时记录待清理状态 | 不备份 |
| 其他缓存 | 可重新生成的下载片段、请求缓存 | 限额和过期清理 | 不备份 |
| 日志 | 状态和错误 | 限制大小、自动轮换、隐去秘密 | 通常不备份 |

### 7.2 空间控制

- 有平台字幕时不下载视频或音频。
- 本地 MP4 有外置或内嵌字幕时不提取音频；原视频不计入成果库或缓存占用，也不被清理命令管理。
- 无字幕时只取得满足转写要求的最低音频质量；链接输入不保存完整视频，本地输入不另存完整视频副本。
- 开始前显示视频时长、预计音频空间、转写后端和处理方式；使用按量服务时显示预计费用及本次程序侧提交门槛。
- 链接输入用 BV 号和分P、本地输入用 SHA-256 形成稳定内部 ID，避免重复下载、重复转写和重复付费；哈希只在首次处理或文件变化后重新计算。
- 临时音频的删除时间以任务最后成功使用时间为准，满 5 天后进入可清理状态。
- 提供“查看占用”“列出待清理项”和“清理过期缓存”命令。
- 清理命令永远不删除五个固定输出文件；执行前展示确切范围。

### 7.3 关键运行文件

影响工具正常运行、迁移时必须理解的内容是：

- 源代码和依赖锁定文件。
- 不含密钥的配置模板。
- 用户实际使用的 `config.ini`；它决定数据目录、切片阈值和默认模型方案，丢失不会损坏旧成果，但新任务会退回模板默认值。
- 文本任务路由、后端配置名、模型、推理档位、转写后端、费用提交门槛和数据根目录等非秘密运行配置。
- 提示词及输出格式版本。
- 用户自己的 DeepSeek/转写 API 密钥或 Cookie；丢失后需重新配置，但不会损坏已有总结。
- `source.json` 和任务状态；本地输入还记录用户原始路径、规范化 WSL 路径、文件大小、修改时间和 SHA-256。丢失后仍能阅读 Markdown，但无法可靠去重、复现或断点恢复。

### 7.4 迁移和恢复

迁移电脑时分三组处理：

1. 从 Git 恢复程序、文档、提示词和依赖版本。
2. 从独立备份恢复整个用户成果目录。
3. 按需要从用户自己的视频库备份恢复本地原视频；成果库不会替用户备份这些大文件。
4. 恢复用户运行配置，并检查新电脑的数据路径和当前可用模型；不能未经检查沿用旧电脑的绝对路径。
5. 从密码管理器重新配置 API 密钥；不迁移缓存和日志。

第一版发布前增加一个只读清单命令和一个导出清单，明确“必须备份 / 可选备份 / 不要备份”。恢复演练是验收项，不能只写文档不测试。

用户已确认将成果保存在 WSL，并默认使用独立的 `/home/dev/bili-summary-data/`；数据根目录仍做成可配置项。代码仓库和成果目录分离的动机是降低误删风险；迁移时需要同时备份代码仓库和该成果目录。

## 8. Codex、DeepSeek 与语音转写方案

### 8.1 需要区分的四类服务

| 服务 | 当前账号能否使用 | 在本项目中的作用 |
|---|---|---|
| ChatGPT Plus 网页/应用 | 能 | 人工复核或临时补充；没有稳定的程序调用接口 |
| Codex CLI 的 ChatGPT 登录 | 当前环境已确认能用 | 已验证的默认文本后端和多模型改造回归基线 |
| DeepSeek API | 项目密钥已在本机秘密文件配置，Flash/Pro 真实调用通过 | 可按文本任务选择模型和推理强度的在线后端；启用对应路由时字幕会发送到 DeepSeek |
| OpenAI Platform API | 当前未确认有独立余额，Plus 不提供 API 余额 | 暂不作为第一版依赖；未来充值条件具备时再接入 |

ChatGPT Plus 订阅和 OpenAI Platform API 是两套计费与访问路径。Plus 不能直接充当 Audio Transcriptions API 的余额。官方 [OpenAI API 快速入门](https://developers.openai.com/api/docs/quickstart)要求创建 API Key，并在平台为持续使用添加 credits。因此，在没有单独 API 充值能力时，不能把 OpenAI 音频或文本 API 设为第一版的必要条件。

### 8.2 已验证的 Codex CLI 文本后端

本机只读探测结果：

- 已安装 `codex-cli 0.150.1`。
- `codex login status` 显示 `Logged in using ChatGPT`。
- 当前 CLI 提供 `codex exec`、只读沙箱、临时会话、JSONL 和结构化输出功能。

官方 [Codex 非交互模式文档](https://learn.chatgpt.com/docs/non-interactive-mode)说明，`codex exec` 可以在脚本和流水线中运行，并可将输入整理为摘要或结构化输出。当前已实施路径是：

1. 主程序负责字幕、切段、时间戳和文件管理。
2. 每个文本任务通过 `codex exec` 发送给当前 ChatGPT 登录的 Codex。
3. Codex 只读处理输入，并通过 `--output-schema` 返回结构化结果。
4. 主程序验证字段和时间戳，再写入最终文件。
5. 使用 `--ephemeral`，避免额外保存 Codex 会话记录。

安全约束：

- 使用 `--sandbox read-only`；模型无权修改项目文件，最终文件由主程序写入。
- 字幕属于不可信输入，提示词明确要求把其中的命令式语言当作课程内容，防止提示注入。
- 不把 `~/.codex/auth.json` 复制进项目、日志或备份；该文件相当于账号凭据。
- 记录 Codex 返回的 token 用量和失败原因，但不把订阅额度换算成虚构的人民币成本。

取舍：此路线无需海外充值，能够实现文本流程自动化；它消耗 ChatGPT/Codex 订阅额度，可能遇到动态使用上限，而且长期可用性受 Codex 产品变化影响。官方文档把 API Key 视为自动化的常规选择，把 ChatGPT 账号认证列为高级路径；本项目仅在个人、可信任的本机环境采用账号认证，不把它推广成远程服务。因此仍保留模型适配器，未来可以替换为其他在线 API 或本地模型。

### 8.3 DeepSeek 文本后端与模型选择

DeepSeek 的 HTTP 接口沿用 OpenAI Chat Completions 生态形成的兼容请求格式，官方给出的基础地址是 `https://api.deepseek.com`。这使本项目可以用 Python 标准库发送普通非流式 HTTPS 请求，不必为了一个接口提前安装 OpenAI SDK 或其他第三方依赖。

截至 2026-08-30，[DeepSeek API 文档首页](https://api-docs.deepseek.com/)和[模型与价格页](https://api-docs.deepseek.com/quick_start/pricing)列出：

| 模型 ID | 本项目拟定用途 | 第一版选择器状态 |
|---|---|---|
| `deepseek-v4-flash` | 整理、总结和低延迟对比 | 可选 |
| `deepseek-v4-pro` | 深度审校和质量优先对比 | 可选 |
| `deepseek-v4-flash-vision-exp` | 图文/视觉实验 | 暂不显示；当前流水线只发送字幕文本，启用它不会带来画面信息 |

模型 ID 不写死在业务逻辑中，而是保存在命名配置里；文档记录当时验证过的模型快照，运行前的 `doctor` 检查负责报告模型是否仍可用。这样 DeepSeek 更新产品名时只需调整配置和兼容性测试。

根据官方[思考模式指南](https://api-docs.deepseek.com/guides/thinking_mode)，项目对用户只暴露四个清晰档位：

| 项目档位 | DeepSeek 请求映射 | 含义 |
|---|---|---|
| `off` | `thinking.type=disabled` | 不启用思考模式 |
| `low` | `thinking.type=enabled`，`reasoning_effort=low` | 较少推理 |
| `high` | `thinking.type=enabled`，`reasoning_effort=high` | 官方默认思考强度 |
| `max` | `thinking.type=enabled`，`reasoning_effort=max` | 质量优先，允许更多推理 |

DeepSeek 会把 `medium`、`high`、`xhigh` 映射到同一 `high` 档，因此本项目不制造三个实际效果相同的选项。思考模式下温度等采样参数被忽略，配置界面也不展示无效旋钮。模型和推理强度是两个正交配置：用户可以分别选 `flash/pro` 与 `off/low/high/max`，但 `doctor` 必须验证组合是否仍被当前 API 接受。

建议配置形态如下，名称代表一套可复用配置，密钥不写在此文件：

```ini
[text_routes]
organize = deepseek_flash_high
summary = codex_default
basic_audit = deepseek_pro_high
deep_audit = deepseek_pro_high

[text_profile.deepseek_flash_high]
driver = deepseek_http
model = deepseek-v4-flash
reasoning = high

[text_profile.deepseek_pro_high]
driver = deepseek_http
model = deepseek-v4-pro
reasoning = high
```

上例是用户在首次真实对比及自行比较 Codex 后确认的长期默认：Flash high 负责忠实整理，Codex high 负责总结，Pro high 负责审校。用户已说明 DeepSeek 费用无需作为选型限制，因此文本 API 不设置人民币阻断门槛，但仍记录输入、输出、推理 token、缓存命中、耗时和按调用时价格快照计算的费用估算。保留记录的动机是复现、性能比较和发现异常重试，厂商账单仍是最终依据。

DeepSeek 的 JSON 模式保证返回合法 JSON，但不保证完全符合本地 JSON Schema；官方[JSON 输出指南](https://api-docs.deepseek.com/guides/json_mode)也提示偶发空内容。因此主程序必须继续执行字段、类型、时间戳和全文覆盖验证，并把空内容、截断和 Schema 不符作为可重试错误。`402` 欠费、`429` 限流、`500` 服务错误和 `503` 过载按官方[错误码说明](https://api-docs.deepseek.com/quick_start/error_codes)归一化记录，断点恢复不能重复消费已成功切片。

API 返回的 `reasoning_content` 默认不落盘，只保存推理 token 数。原因是原始思考内容会增加敏感数据和存储膨胀，也不是生成最终学习资料所需的可复现输入。

### 8.4 模型调用前的选择与确认

日常交互命令采用以下时序：

```text
bili-summary run "网页链接"
        ↓
解析链接并取得视频信息、字幕或统一文本
        ↓
估算切片数，解析本次任务的默认文本配置
        ↓
终端一次性显示并允许修改模型、推理强度和审校档位
        ↓
用户确认后才开始 Codex 或 DeepSeek 文本调用
```

取得平台字幕属于输入准备，可以发生在选择之前；任何字幕内容发送给 Codex 或 DeepSeek 都必须发生在确认之后。选择界面只出现一次，后续所有切片自动沿用本次已确认方案，避免长视频每片重复询问。

交互界面至少显示：视频标题与时长、文本来源、预计切片数、`off/basic/deep` 审校档位，以及整理、总结、Basic 审校、Deep 审校分别解析到的后端、模型和推理强度。默认操作如下：

```text
[Enter] 使用配置文件中的默认方案
[1] 修改本次任务
[2] 使用质量优先预设
[3] 使用速度优先预设
[q] 取消，不调用文本模型
```

配置和覆盖分为三层，优先级从高到低为“本次交互选择或命令行参数 → 命名预设 → `config.ini` 默认值”：

1. `config.ini` 保存长期默认路由和命名预设，不保存 API Key。用户日常不需要手工编辑它。
2. 终端交互只覆盖当前任务，不静默回写 `config.ini`；若用户希望以后都采用同一方案，再通过明确的配置命令或手工修改保存。
3. 自动化脚本可以使用 `--profile "quality" --yes` 直接确认某个整体预设，也可以重复使用 `--route "summary=deepseek_summary"` 进行逐任务覆盖。`--yes` 只跳过交互确认，不绕过配置和能力验证。

在交互终端中，默认进入一次性选择界面；非交互环境若没有 `--yes`，程序应停止并提示完整命令，不能等待一个永远不会到来的键盘输入。模型与推理组合不可用、DeepSeek Key 缺失或审校档位与路由冲突时，必须在发送字幕前报错。

本次确认后的完整方案立即写入任务状态，并在成功后进入 `source.json`。断点续跑必须沿用原方案；如果用户改换模型或推理强度，相关任务使用新的缓存指纹重新执行，不能把新旧模型结果混成同一次可追溯运行。

### 8.5 国内在线语音转写核实结果

确实存在适合本项目的国内在线 API。ASR 是 Automatic Speech Recognition（自动语音识别）的缩写；这一产品类别早于大模型，近年云厂商在传统录音文件识别之外增加了大模型识别、Prompt 上下文和时间戳能力。本项目只需要“录音文件非实时识别”，无需引入实时 WebSocket 流式识别。

截至 2026-08-31，官方资料给出的候选如下。价格和免费额度可能调整，实施时必须以登录控制台后的实际计费页再次确认。

| 候选 | 官方能力与当前公开价格 | 对本项目的判断 |
|---|---|---|
| 阿里云 Qwen3 ASR Flash Filetrans | [官方选型](https://help.aliyun.com/zh/model-studio/asr-model/)列出的新一代非实时文件转写型号；支持 12 小时/2 GB、句级及可选字级时间戳，不支持热词或 Prompt；北京原价 0.00022 元/秒 | 作为基础准确率主候选；用户已在控制台确认 `qwen3-asr-flash-filetrans` 免费额度可用 |
| 阿里云百炼 Paraformer v2 | [计费页](https://help.aliyun.com/zh/model-studio/model-pricing)当前为 0.00008 元/秒，即 0.288 元/小时，并每月发放 10 小时额度；[非实时 API](https://help.aliyun.com/zh/model-studio/paraformer-recorded-speech-recognition-restful-api)支持文件转写、时间戳校准和热词 | 用户控制台已确认剩余 36,000 秒；作为较早一代的低成本对照和备用 |

2026-08-31 用户将在线范围收敛为同一阿里云业务空间内的 Qwen3 Filetrans 与 Paraformer v2，不再比较豆包。阶段 4 使用同一段 10 分钟音频比较：

1. 中文及中英混合术语准确率。
2. 句段和词级时间戳能否稳定转成 SRT。
3. 提交到取回结果的实际耗时。
4. 厂商返回的计费用量、公开价格估算和是否发生额外对象存储费用。
5. API Key 配置复杂度、账号实际可调用性、失败重试和临时云端音频生命周期。

用户已经登录北京地域百炼。下一步只需创建本项目专用 API Key 并安全保存到本机；无需注册豆包或腾讯云。程序设置提交前的人民币费用门槛且默认不自动充值。这个门槛能阻止程序发起预计超额的新任务，但云厂商掌握最终计费，不能描述为绝对账单上限。两个候选共用标准库 HTTP 客户端和异步任务结构；比较后默认只选一个模型，不引入 DashScope SDK。

第一版无字幕分支据实测结果采用以下顺序：

1. `auto` 模式在已配置在线服务且预计费用不超过程序侧提交门槛时使用胜出的国内 API。
2. 在线服务未配置、超预算、不可用或用户指定 `--transcriber "local"` 时，使用本地 CPU 转写。
3. 两种自动方式都失败时，生成可人工处理的音频说明包，并允许导回 SRT 或统一 JSON。

本地转写因此保留为隐私和可用性兜底，不再预设为无字幕视频的固定首选。调用国内在线转写时，音频会离开本机；若厂商要求对象存储，成功取回结果后删除本项目创建的临时云端对象，但厂商服务端的内部保留政策仍以其协议为准。

ChatGPT Plus 界面仍不能作为本工具的自动转写后端。OpenAI API 转写也只保留为未来候选：根据 [OpenAI 文件转写文档](https://developers.openai.com/api/docs/guides/speech-to-text)，单文件上限为 25 MB，长音频需要压缩或切片；[GPT Transcribe 官方模型页](https://developers.openai.com/api/docs/models/gpt-transcribe)当前标价为 0.0045 美元/分钟，约 0.27 美元/小时。当前缺少独立 API 支付路径，因此不进入第一版默认实现。

### 8.6 知识审校与额度控制

- `basic` 只检查明显风险，不像 `deep` 那样用独立大窗口再次完整扫描全文；阶段 3.1 后它拥有独立路由和结构化结果，便于单独选择模型并衡量误报。
- `deep` 使用其任务路由所选文本后端单独重审全文，开始前显示后端、模型、推理档位、预计切片数和上下文规模。
- Codex 订阅使用没有稳定的人民币单价，记录 token、调用次数、耗时和限额错误；DeepSeek 记录 token、缓存命中、耗时和按价格快照计算的费用估算。
- 发生订阅限流、API 限流或临时服务错误时保留已完成切片，可稍后续跑，不能重做全部任务。
- 国内转写属于按量 API；开始前显示预计费用，设置程序侧提交前费用门槛，超出时停止提交新任务并保留现场。厂商侧同时关闭自动充值并按其能力设置预算提醒。

### 8.7 时间戳精度策略

- 平台字幕：使用平台原时间戳。
- 国内在线转写：优先保存厂商原始句段/词级时间戳，再转换为 SRT；`source.json` 记录厂商、模型和转换版本。
- 本地转写：保留句段级时间戳并校正切片偏移。
- 人工导回字幕：要求 SRT 或统一 JSON；只有全文、没有时间戳时标记为粗略来源。
- 只返回全文文本的在线服务不能成为默认转写后端。
- 审校报告中的每条风险至少指向一个可点击的句段起点。

## 9. 阅读软件和后续导出

### 9.1 Typora 探测结论

已只读确认 Windows 侧 Typora 安装在：

```text
D:\Programs\Typora\Typora.exe
```

当前 WSL 内没有发现 Linux 版 Typora，也不需要重复安装。Windows Typora 可以通过 `\\wsl.localhost\Ubuntu\...` 直接读取 WSL 文件。

这种跨系统访问确实存在文件系统转换开销，但本项目的 Markdown 通常是数百 KB 到数 MB，主要是顺序打开、保存，开销相对模型处理和转写时间可以忽略。需要避免的是让 Windows 软件频繁扫描 WSL 中成千上万个小文件；本项目每个视频只有少量成果文件，不属于这种场景。

第一版采用以下边界：

- 代码、任务状态和长期成果都留在 WSL，保持 Linux 工具运行效率。
- Typora 只在用户查看或编辑单个 Markdown 时跨系统访问。
- 主程序提供 `open` 命令，把 Linux 路径转换为 Windows 路径并调用现有 Typora。
- 主程序另提供按需 `export` 命令，将选定成果复制到 Windows 目录，供幕布导入或后续 PowerPoint 编辑。
- 导出副本不作为主数据，避免 WSL 和 Windows 两边修改后无法判断哪份最新。

阶段 1 会用一份测试 Markdown 实测首次打开、保存和再次读取；若实际延迟明显，再改为导出后阅读。重复安装 Linux Typora 仍不会改善跨系统成果展示，还会增加配置和许可维护。

### 9.2 幕布与 Obsidian

- 当前以标准标题、列表、代码块和 LaTeX 生成 Markdown，方便 Typora 阅读和幕布导入。
- 第一版不依赖 Obsidian。
- 未来 Obsidian 适配器可添加 YAML 属性、标签、双向链接和知识索引；正文仍保持标准 Markdown。
- 幕布若无法从 WSL 的 UNC 路径直接导入，就先通过 `export` 复制单个文件到 Windows；不迁移整个成果库。

### 9.3 PPT 扩展

PPT 作为独立阶段和小工具处理，建议流程是：

```text
完整整理稿 + 学习总结
          ↓
演示大纲（逐页标题、要点、讲稿、素材需求）
          ↓
人工/模型审核
          ↓
PPTX 生成器
```

这样能在生成文件前先审阅叙事结构，避免直接产出难以修改的幻灯片。第一版为此保留稳定的章节、公式、案例和来源时间戳结构，不现在引入 PPT 库。

## 10. 测试样例

v1.1 沿用三个主要链接样例和一个边界样例，并增加一个用户现有的本地 MP4；无需为了本地入口额外收集其他文件。

| 角色 | 链接或文件 | 用户提供/已检查的特征 | 主要验证目标 |
|---|---|---|---|
| 主样例 A | `BV1pb8o6yE8f` | 约 1 小时 40 分，AI 字幕 | 长字幕获取、全文审校、长文本总结 |
| 主样例 B | `BV1fKtN6DErG` | 约 3 分钟，AI 字幕 | 最短端到端路径和输出格式 |
| 主样例 C | `BV1N8w9zJENu` | 约 1 小时 25 分，无字幕选项 | 音频获取、国内 API 与本地转写比较、耗时和资源控制 |
| 边界样例 D | `BV1FwY4zkEef` | 约 3 小时 57 分，有字幕但无字幕选项卡 | 极长文本、隐藏字幕可用性、切段和恢复 |
| 本地样例 E | `D:\Downloads\04_公考事业编\视频课程\行测\20250828 片段刷题4.mp4` | 标准 MP4，约 266 MiB，WSL 可读；轨道信息待 `ffprobe` 检查 | Windows/WSL 路径、文件哈希、字幕轨、直接跨系统读取和原文件保护 |

边界样例不进入每次回归测试，只在长流程稳定后运行一次。实施阶段会先读取页面确认实际状态；平台字幕状态可能随时间和账号访问条件变化。

## 11. 分阶段实施计划

每阶段结束后展示交付物并等待确认，再进入下一阶段。

### 阶段 0：需求定稿（已完成）

- 记录已确认的 Codex 后端、`basic` 默认审校和独立数据目录。
- 已确认命令空格规则和国内在线转写选择流程。
- 已只读检查 CPU、内存、显卡接口、磁盘、Python、`ffmpeg` 和 Git 现状，结果记录在第 13 节。
- 本机转写的真实速度必须用相同音频和实际模型测量，因此推迟到阶段 4；未提前下载语音模型、创建云账号、上传音频或产生费用。
- 定稿第一版验收标准。

交付物：v1.1 需求计划和技术选择记录。
停止点：用户明确说“可以开始实施”后才进入阶段 1。

### 阶段 1：最小项目骨架（已验收）

- 选择语言和依赖管理方式，创建项目内隔离环境。
- 确认当前目录的 Git 状态；当前 `.git` 尚未构成可用仓库，得到实施授权后再初始化或修复。
- 建立代码、测试和配置结构；经用户批准后创建独立成果目录 `/home/dev/bili-summary-data/`。
- 添加 `.gitignore`、配置模板和最小使用说明。当前第三方依赖为零，因此不制造空的锁定文件；引入第一项真实第三方依赖时再生成锁定文件。
- 实现不联网的链接解析，以及 `run-file` 的 Windows/WSL 路径规范化、MP4 后缀检查、可读命名和文件身份测试；先使用小型临时测试文件，不读取真实视频内容。
- 建立通用转写适配器和费用提交门槛配置，但暂不安装任何厂商 SDK。
- 建立 Codex 文本适配器的结构化输出模式，但先用模拟结果测试，不消耗订阅额度。
- 验证 Windows Typora 打开一个测试 Markdown；不安装 Linux Typora。

交付物：帮助命令、离线测试和目录样例。
停止点：审阅依赖、文件位置和备份边界。

2026-08-30 实施结果：

- 使用 Python 3.10 标准库和项目内 `.venv`；虚拟环境未安装 pip 或第三方包，占用约 52 KiB。
- 已实现 `run`、`run-file` 和 `doctor` 的阶段 1 离线行为；真实 MP4 预览只读取文件状态，默认不计算全文哈希或媒体轨。
- 已建立只返回模拟结果的转写/文本适配器，以及只构造命令、不执行的 Codex 结构化输出适配器。
- 15 项 `unittest`、Python 编译检查、Bash 语法检查和 JSON Schema 语法检查全部通过。
- 已创建初始提交 `34b1f26`（`chore: establish stage 1 project skeleton`）；仓库当前没有远端地址，提交只保存在本机 `.git`。
- 已创建空的 `/home/dev/bili-summary-data/`，没有写入测试成果或复制视频。
- Windows Typora 启动命令已无错误返回并指向 WSL 内的 `README.md`；是否在桌面正确显示仍需用户目视确认。

### 阶段 2：平台字幕与短视频通路（已验收）

- 处理主样例 B。
- 获取元数据和平台 AI 字幕。
- 输出 `字幕.srt` 和 `source.json`。
- 通过只读、临时的 `codex exec` 生成完整整理稿、`basic` 审校报告和学习总结。
- 记录一次真实 token 用量、订阅调用次数与耗时。

交付物：3 分钟视频的完整文件集。
停止点：逐文件核对格式、原意、时间戳和命名。

2026-08-30 实施结果：

- 已用官方域名公开接口获取样例 B 元数据：标题“失败的AI Slop数字永生尝试”，单 P，时长 169 秒。
- 未登录的 `x/player/v2` 与 `x/player/wbi/v2` 当前均返回 0 条字幕；流程将“无可见字幕”作为正常停止状态，在写成果和调用 Codex 前退出。
- 已实现项目标准库平台适配器、字幕 JSON 到 SRT 的毫秒时间戳转换、原子成果写入、任务状态与 Codex JSONL 用量解析；仍为零第三方 Python 依赖。
- Codex 命令固定使用 `read-only`、`--ephemeral`、`--output-schema` 和 `--json`；复用现有 CLI 登录态，不需要 OpenAI API Key。
- 新增本地 `config.ini` 和 `.secrets/bilibili_cookie.txt` 入口；二者均被 Git 忽略，Cookie 不写入日志或 `source.json`。程序不自动读取浏览器凭据。
- 21 项离线测试通过；真实无 Cookie 失败路径退出码为 2，成果目录保持为空，未启动 Codex。
- 用户已在权限为 `600` 的 Git 忽略文件中配置 B 站 Cookie；登录接口返回中文平台字幕 88 段，覆盖 `00:00:00.100` 至 `00:02:48.520`。Cookie 内容未进入日志或 `source.json`。
- 已生成固定的六项成果：SRT、原始字幕 JSON、完整整理稿、`basic` 审校报告、学习总结和 `source.json`，总计约 31 KiB；没有下载视频、音频或产生媒体缓存。
- 真实 Codex 调用 1 次，模型 `gpt-5.6-sol`，CLI `0.150.1`，只读临时会话耗时 63.065 秒；输入 17,373 token，输出 2,775 token，其中推理输出 262 token。
- 重复执行返回 `already_complete`，耗时约 0.35 秒，任务记录仍为 1 次调用；只有显式 `--force` 才允许再次生成。
- 阶段 2 校准后的后续验收阈值：字幕转换必须保持 100% 有效句段且时间单调；关键事实误写数为 0；每个主要主题至少在整理稿中出现一次，关键主题遗漏数为 0；审校报告中的风险项必须 100% 带可回看时间点；任何无法仅凭字幕核实的修正必须进入警告。可读性继续由用户人工验收，不以程序指标替代。
- 用户已逐文件验收阶段 2 成果；阶段 2 代码提交后进入阶段 3。
- 阶段 2 提交为 `c816d5b`（`feat: complete stage 2 subtitle pipeline`）。

### 阶段 3：长字幕、全文审校与总结

- 处理主样例 A。
- 按语义和时间切段，保持全文覆盖和时间戳映射。
- 分开执行文本审校和学习总结，并比较一次 `basic` 与 `deep` 审校。
- 减少跨段重复，核对重要结论的字幕依据。
- 加入订阅限流恢复、切片缓存和重复调用防护。

交付物：约 1 小时 40 分视频的完整文件集。
停止点：按错误、遗漏、时间戳、订阅用量、耗时和可读性验收。

2026-08-30 实施结果：

- 主样例 A 当前标题为“欢迎来到未来 [01-Raw/26生成式软件工程/NJU]”，时长 6,000 秒；登录接口返回中文平台字幕 2,552 段、30,592 个字幕字符，覆盖 `00:00:00` 至 `01:39:58.390`。没有下载视频或音频。
- 普通流程按停顿分成 6 个约 15～20 分钟主片；每片一次生成完整整理稿片段、Basic 风险项和总结笔记，再用 1 次调用合并总结。Deep 使用独立提示词把原始字幕分成 2 片重新覆盖全文。
- 四个分片阈值已进入 `config.ini` 的 `[long_processing]`，默认普通目标/上限为 15/20 分钟，Deep 为 50/55 分钟；仍为零第三方 Python 依赖。
- 每个成功步骤以提示词、Schema 和模型相关指纹写入 `/home/dev/bili-summary-data/.bili-summary-cache/<任务ID>/`；缓存不保存完整提示词或 Cookie。模拟“主片成功、总结合并失败”后，续跑只执行剩余步骤，最终重复运行不调用模型。
- 23 项离线测试通过，包含切片全文保持、中断恢复、缓存复用、时间戳范围、秘密文件隔离和完成任务去重。
- 真实执行共 9 次 Codex 调用，模型 `gpt-5.6-sol`、CLI `0.150.1`；累计耗时 1,501.204 秒，输入 284,582 token，输出 69,714 token，其中推理输出 18,265 token。重复执行耗时约 0.5 秒，新增调用为 0。
- 成果目录约 772 KiB，结构化恢复缓存约 224 KiB；缓存标记为“不要备份”，阶段 3 验收前保留，之后由后续清理清单管理，避免长期无界增长。
- 整理稿 27,884 字，约为原字幕字符数的 91%；包含 218 个时间点，十个 10 分钟区间全部覆盖，未发现完全重复的长段落。学习总结含 11 个二级章节。
- Basic 标记 70 个风险项（61 个疑似字幕错误、9 个疑似讲者知识错误）；Deep 标记 185 个（153 个疑似字幕错误、32 个疑似讲者知识错误）。Deep 数量更多只代表更激进的覆盖，可能增加误报，需人工返回视频核对。
- 9 个模型结果合计产生 52 条去重前警告；程序已把去重后的警告写入 `source.json`，Cookie 未出现在成果或缓存中。
- 待用户核对全文原意、主要知识遗漏、Basic/Deep 误报、总结层级、时间戳和可读性后，才提交阶段 3 并进入阶段 4。

### 阶段 3.1：多模型文本路由与运行前选择（已验收提交）

此阶段插在阶段 3 验收提交之后、媒体与转写依赖进入项目之前。原因是它只改文本处理边界，可以用已有字幕和成果基线验证，不应与 FFmpeg、ASR 账号和音频上传同时引入。

实施顺序：

1. 先保留阶段 3 的 Codex 成果和调用指标作为回归基线；未经用户验收不重写该成果。
2. 把当前偏 Codex 的结果元数据改为通用文本调用记录，并将整理、总结、Basic 审校和 Deep 审校拆成独立提示词、Schema 与路由。
3. 增加命名文本配置和逐任务路由；默认全部指向 `codex_default`，证明改造不改变旧命令的默认行为。
4. 用 Python 标准库实现非流式 `DeepSeekHttpBackend`；不安装 OpenAI SDK。先用模拟 HTTP 响应测试 JSON、空响应、截断、限流、欠费、服务故障和断点恢复。
5. 在取得字幕、估算切片之后，加入首次文本模型调用前的一次性交互选择；测试取消、采用默认值、本次覆盖、命名预设和非交互 `--yes` 路径。
6. 用户之后只在本机 Git 忽略的权限 `600` 秘密文件或环境变量中填写 DeepSeek API Key；密钥不通过聊天传递。配置前不发出真实调用。
7. 用同一短字幕切片比较 `flash/pro` 与有意义的推理档位，分别评估整理忠实度、总结结构、审校漏报/误报、耗时和 token；费用只记录，不作为质量选型门槛。
8. 确认本次选择、缓存指纹、`source.json` 和日志能区分任务、后端、厂商、模型和推理档位；默认不保存 DeepSeek 原始 `reasoning_content`。

交付物：通用文本后端接口、DeepSeek 稳定适配器、逐任务配置、运行前选择界面、非交互自动化入口、离线错误测试，以及一份同切片对比记录。

停止点：由用户确认各任务的默认文本配置；未确认前保持 Codex 默认，不进入阶段 4。

2026-08-30 实施结果：

- 阶段 3 已先提交为 `42ff259`（`feat: complete stage 3 long subtitle pipeline`），原 Codex 成果与 9 次调用指标保持不变。
- 整理、总结、Basic 审校和 Deep 审校已拆为独立提示词、Schema、命名配置和路由；默认四项均为 `codex_default`。
- Codex CLI 配置可记录并覆盖模型与推理档位；DeepSeek 使用 Python 标准库直接调用非流式 Chat Completions JSON 模式，没有引入 SDK 或第三方依赖。
- 运行前选择支持默认、单次修改、质量/速度预设、取消、`--profile`、重复 `--route` 和非交互 `--yes`。DeepSeek 密钥缺失或文件权限不是 `600` 时，会在成果写入及首次模型调用前停止。
- 缓存指纹包含任务、命名配置、driver、模型、推理档位、提示词版本和 Schema；`source.json` 使用通用 `text_plan` 与逐调用 `text` 记录，不保存 `reasoning_content` 或密钥。
- 38 项离线测试通过，覆盖默认兼容、用户确认的逐任务路由、知识/逻辑审校边界、无效审校类别拒绝、跨配置缓存隔离、中断恢复、对比缓存，以及 DeepSeek 有效 JSON、空响应、空内容、截断、402 欠费、429 限流、500 错误和 503 过载。
- DeepSeek Key 已保存到权限 `600`、Git 忽略的秘密文件；最小认证调用成功，密钥未输出或写入成果。
- 同一份 88 段、986 字符、约 2 分 49 秒的字幕已完成 Flash low 与 Pro high 的四任务对比，共 8 次真实调用。所有结果通过结构和时间戳校验，再次执行 8 项均复用缓存。
- Flash 四项合计 68.217 秒、估算 0.00812196 美元；Pro 合计 284.720 秒、估算 0.04636830 美元。Pro 约为 4.17 倍耗时、5.71 倍费用，输出字符多 18.8%；费用和长度只作为效率事实，不直接代表质量。
- 抽查中 Pro 总结更完整且少一处无依据扩写；其 Deep 审校也出现“福岛→富士康”的激进推测。Flash 整理更保守，Basic/Deep 同样存在口语误报。基于本样本的 DeepSeek 混合倾向是整理、Basic、Deep 用 Flash，Summary 用 Pro。
- 用户结合自己的 Codex 对比后确认长期默认：整理使用 `deepseek_flash_high`，总结使用 `codex_default`，Basic/Deep 审校使用 `deepseek_pro_high`。用户判断优先于上面的单样本工具侧倾向。
- 审校目标同步收紧为知识与逻辑风险；普通口语、语法和文风猜测不再上报，字幕错误仅保留会改变术语、公式、数字、单位、否定关系或逻辑含义的高置信问题。新提示词和 Schema 会改变缓存指纹，旧对比结果继续作为历史证据，不会被静默覆盖。
- 对比事实、抽查判断和最终决定分别保存在 `对比说明.md`、`对比指标.json` 与 `质量观察.md`。

### 阶段 4：本地 MP4 与无字幕转写

- 处理主样例 C。
- 安装前再次确认 Ubuntu `ffmpeg` 软件包的内容和影响；经用户授权后只安装该媒体工具包，不安装 VLC、mpv 或其他播放器。
- 先用 `ffprobe` 只读检查本地样例 E 的编码、时长、音轨和字幕轨，并检查同目录同名 SRT。
- 验证直接读取 `/mnt/d/...` 的速度；只有明显成为瓶颈时才创建 WSL 临时副本，并记录副本的清理时间。
- 若本地样例有字幕，直接进入统一文本流程；若没有字幕，复用下面相同的音频提取和转写流程。无论哪条路径都不得修改、移动或删除原 MP4。
- 只取得最低需求音频，并验证 5 天保留策略。
- 先从同一段音频截取 5～10 分钟样本；调用前重新核对 Qwen3 Filetrans 和 Paraformer v2 的控制台价格、免费额度、文件限制和数据条款。
- 用户在华北 2（北京）百炼业务空间创建仅供本项目使用的 API Key；Workspace ID 可进入本机配置，API Key 只能进入权限 `600` 的 Git 忽略文件或环境变量。
- 优先使用用户已确认的两项免费额度测试两个阿里云模型。仍按付费最坏情况估算：10 分钟两项合计理论上限 0.18 元，并通过程序侧 1 元提交门槛和用户确认。
- 用相同片段测试本地 CPU 转写，按术语准确率、时间戳、等待时间、厂商用量估算和配置复杂度形成比较表。
- 两个模型共用一个标准库异步 HTTP 适配器；比较后只将胜出模型设为默认，不安装厂商 SDK。若两项效果都不合格，维持本地默认。
- 只有胜出服务的免费额度不足以处理完整样例时，才讨论是否转正式版或充值；该操作需要新的明确授权。
- 用选定转写后端处理完整主样例 C，并把结果送入同一套可路由的文本整理、审校和总结流程；同时验证在线转写失败或超预算时回退本地。
- 第一版不生成面向 ChatGPT 或其他服务的人工上传包：自动 Qwen 转写已经跑通，继续维护人工音频包和字幕导回格式会形成重复通路并增加临时大文件；以后只有自动服务不可用且出现真实需求时再单独设计。
- 测试长文件限制、必要切片、重叠去重、失败重试、断点恢复和临时云端对象清理。

交付物：本地 MP4 媒体检查记录、厂商与本地转写比较记录、厂商用量及公开价格估算、字幕和完整学习文件集。实际免费额度或账单扣减不作为验收项。
停止点：确认无字幕默认策略。

2026-08-31 阶段 4A 实施进度：

- 当前系统已存在 Ubuntu 22.04 的 `ffmpeg`/`ffprobe 4.4.2`；本次恢复检查没有执行安装，也无法仅凭当前状态判断此前由谁安装。
- `run-file --probe` 已实现只读媒体检查：同名外置 SRT 优先，其次识别可提取内嵌字幕；无字幕时确认音轨是否存在。
- 本地样例 E 已实测为 5,171.153 秒、279,202,588 字节，包含 H.264 视频和 AAC 音频各一轨，无外置或内嵌字幕，需要语音转写。
- 对 `/mnt/d/...` 原文件的 266 MiB 顺序读取耗时约 1.6 秒，当前没有为性能创建 WSL 视频副本的理由。
- `run-file --prepare-audio-sample` 已实现：自动计算源文件 SHA-256，只在没有可用字幕时生成 5～10 分钟、单声道 16 kHz、16-bit PCM WAV；原 MP4 不修改、不移动、不复制。
- 样本位于数据根目录的隐藏恢复缓存；同一源文件和提取配置使用指纹复用。元数据记录最后成功使用时间、5 天后的可清理时间和 `do_not_backup`，当前阶段只标记、不自动删除。
- 58 项离线测试通过。真实样例已生成 24:00～34:00 的 600 秒、19,200,078 字节 PCM WAV，`ffprobe` 复核为单声道 16 kHz、16-bit；FFmpeg 实际转码约 0.88 秒，原 MP4 的大小和修改时间保持不变。
- 最初截取的开头 10 分钟以背景音和静音为主，`whisper.cpp` 出现重复幻觉文本，因此改用连续讲课窗口并覆盖旧临时样本。该失败说明评测窗口必须先检查是否含代表性语音。
- 隔离在 `/tmp` 的 `whisper.cpp b4938` 多语种 `small` 已完成相同 10 分钟 CPU 基线：8 线程耗时 1 分 51.32 秒，峰值常驻内存约 933 MiB，无交换，输出 194 个连续时间戳片段。整体语句可读，但“行测”等课程术语和若干同音词仍有明显误写，只适合作为当前兜底候选，需等待两项在线结果再决定默认后端。
- 阿里云两个候选已共用标准库异步 HTTP 客户端；提交参数固定第一音轨，Paraformer 关闭说话人分离，Qwen3 请求字级时间戳且不支持热词或 Prompt。`check-aliyun-asr` 已在不上传音频、不提交转写的情况下确认 API Key 鉴权与两个模型的临时上传策略，且没有输出密钥、策略签名或临时访问凭据。
- 固定样本的在线真实调用已经完成：Qwen3 返回 110 个句段、厂商处理 5.982 秒；Paraformer 返回 99 个句段、厂商处理 6.663 秒。两项时间戳均单调并覆盖到约 10 分钟。三个窗口抽查中 Qwen3 的课程术语与上下文错误更少，用户审阅后已确认其为无字幕视频默认在线候选。
- Qwen3 本轮没有指定 `language`，但原始响应把 110 个句段全部自动判定为 `zh`。`language=zh` 是语种提示而非东北话开关，且官方没有为 Qwen3 提供东北话参数；因此保持自动识别，不增加配置复杂度。只有完整课程反复出现影响知识含义的口音错误时，才重新评估明确支持地区官话或热词的模型。
- 首次 Qwen3 下载因旧通用域名返回 HTTP 结果地址而被安全检查阻断；程序现只接受阿里云结果域名并强制升级 HTTPS。首次 Paraformer 提交发生 SSL EOF，最近 24 小时任务列表确认未创建任务后才重试成功；上传成功状态现在也进入恢复缓存。网络异常留下一个未引用的 Paraformer 私有临时副本，无法即时删除，预计 48 小时后自动失效。

2026-08-31 阶段 4C 实施进度：

- `run-file --execute` 已接入完整流程：同名外置 SRT、可提取内嵌字幕、Qwen3、Paraformer、显式配置的 whisper.cpp 依次作为文本来源；默认仍是无副作用预览。
- 正式全长音频采用第一音轨、单声道 16 kHz、16-bit PCM WAV，缓存记录源 SHA-256、5 天保留策略和 `do_not_backup`；原 MP4 不复制、不上传、不修改。
- 首次以 1 元本机门槛验证费用阻断发生在全长音频提取和提交之前；用户确认额度恢复后只把本机门槛调整为 1.2 元，使 1.137654 元的最坏估算可提交。
- Qwen3 完成长 5,171.153 秒的视频转写，厂商任务处理 40.311 秒，返回 832 个句段并报告计费用量 3,475 秒；按公开原价估算为 0.7645 元，成果保留统一 SRT 和约 2.9 MiB 的厂商原始 JSON。
- 同一字幕分为 6 个主切片，按长期路由完成 19 个文本任务。最后一段审校出现一次 `IncompleteRead` 后，续跑复用了转写和前 17 项缓存，只执行缺失审校与总结合并；DeepSeek 适配器随后增加一次连接级自动重试，重试仍失败时继续由断点状态承接。
- 完成后相同命令约 1.47 秒返回 `already_complete`，没有重新提取音频、调用 ASR 或调用文本模型；源 MP4 的 SHA-256、大小和修改时间保持不变。
- Paraformer 在线回退、本地 whisper.cpp 显式配置与回退均有离线测试；本轮 Qwen3 正常成功，没有额外制造一次完整故障调用。
- 69 项离线测试全部通过，覆盖媒体优先级、全长音频缓存、费用门槛、Qwen/Paraformer 回退、本地 CPU 配置、CLI 正式执行、断点恢复和不完整 HTTP 响应自动重试及失败归一化。
- 用户审阅后决定不重做低信息密度的刷题成果；该样例已经完成本地媒体、在线转写和断点恢复验证，不再承担知识讲座质量基准。整理流程随后增加 `lecture/practice` 内容模式并建立“Basic 审校→整理→分片总结→最终总结”依赖，72 项测试通过。用户确认阶段 4 收口并返回主线；实际免费额度/账单扣减不再补录，人工上传包从第一版范围移除。

### 阶段 5：极长视频与日常可靠性

- 使用边界样例 D 验证接近 4 小时的流程。
- 覆盖多分P、失效链接、受限内容、字幕异常和模型失败。
- 实现磁盘占用查看、5 天过期列表和安全清理。
- 日志隐去密钥、Cookie 和敏感参数。
- 完成备份、迁移、恢复说明并实际演练。

交付物：第一版可用版本和维护手册。
停止点：确认恢复有效且清理不会误删用户成果。

2026-08-31 阶段 5 首批进度：

- 已实现 `cache-status`：只读统计缓存总量、受管临时音频、5天到期时间和当前可清理字节。
- 已实现 `cache-clean` 的默认预览与显式执行边界；实际删除必须使用 `--execute` 并再次确认，且只接受位于 `.bili-summary-cache/local-*/media/`、文件名固定、带 `do_not_backup` 和受支持生命周期的普通文件。
- 清理器拒绝路径不匹配、符号链接、无效时间和未知元数据；不会删除字幕、厂商原始响应、文本恢复缓存或五个固定成果文件。
- 对当前真实数据只执行了只读清单和清理预览：缓存总计 188,135,896 字节，其中受管临时音频 184,677,070 字节；两项音频分别到 2026-09-05 才符合清理条件，因此本轮删除 0 字节。
- 78 项离线测试全部通过，其中覆盖默认预览、到期判断、路径篡改和符号链接目标保护。
- 极长视频、备份清单、迁移恢复演练和维护手册仍待后续阶段 5 工作。

### 阶段 6：按真实需求扩展

优先候选顺序：

1. 独立“学习审核 Agent”：允许联网查证来源、使用更高推理强度，以额外算力换取更深入的知识审核；与日常低成本流水线分离。
2. PPT 演示大纲与独立 PPTX 转换器。
3. Obsidian 元数据、标签和知识索引导出。
4. 本机 UI 和任务历史。
5. 批量队列和定时运行。
6. 视频画面与关键帧分析。

每一项单独立计划、审阅和验收，避免影响已稳定的字幕与总结主流程。

## 12. 第一版验收标准 v1.2

- 能处理三个链接主样例和本地 MP4 样例；边界样例用于阶段 5 的一次性极限验证。
- `run-file` 能接受带空格和中文的 Windows 盘符路径或 `/mnt/<盘符>/...` WSL 路径，并将两者解析到同一文件。
- 本地 MP4 按“同名 SRT、内嵌字幕、音频转写”的顺序处理；自动流程不依赖任何视频播放器。
- 默认不复制、修改、移动或删除本地原视频；若产生 WSL 临时副本，必须被标记为缓存并纳入清理规则。
- 本地文件的 `source.json` 记录原始路径、规范化路径、大小、修改时间和 SHA-256；原文件丢失时清楚说明已有成果可读但无法重跑。
- 用户可见目录以主题和可读标题命名，不以 BV 号或代码数字命名。
- 每个成功任务至少交付 `字幕.srt`、`完整整理稿.md`、`审校报告.md`、`学习总结.md` 和 `source.json`。
- 有平台字幕时不下载完整视频或音频，也不启动语音转写。
- `完整整理稿.md` 覆盖全文；清理口头重复时不能丢失知识点。
- 审校报告注明 `off/basic/deep` 档位和覆盖范围；实际风险项区分“影响知识或逻辑的高置信疑似字幕错误”和“疑似知识或逻辑错误”，并附可回看的时间点，不报告普通口语或文风问题。
- 学习总结包含知识结构、关键概念、公式/步骤、案例和复习问题。
- 同一输入重复执行不会无理由重复下载、重复付费或生成多份大文件。
- 国内在线转写开始前显示厂商、模型、预计费用和程序侧人民币提交门槛；超出门槛时不提交新任务。文档明确该门槛不能替代厂商账单控制。
- 在线转写保存厂商原始时间戳，并能回退到本地转写；`source.json` 可追溯厂商、模型、用量和实际费用。
- 默认文本配置采用用户确认的逐任务路由：Flash high 整理、Codex high 总结、Pro high 审校；各任务仍保持结构、时间戳和缓存隔离验证。
- 整理、总结、Basic 审校和 Deep 审校能分别选择命名文本配置；DeepSeek 配置能选择 `flash/pro` 模型以及 `off/low/high/max` 推理档位。
- Codex 文本任务使用只读沙箱和临时会话；DeepSeek 使用直接 HTTPS API；两者都记录任务、后端、实际厂商、模型、推理档位、token、调用次数、耗时与标准化错误。
- Codex 达到订阅限额或 DeepSeek 出现可恢复错误时，均可从已完成切片恢复，不重复消耗已完成工作。
- 缓存不会跨任务、后端、模型、推理档位、提示词或 Schema 错误复用；`source.json` 可追溯每次调用。
- 交互运行在取得字幕后、首次文本模型调用前只提示一次；取消时不发送字幕，确认后全部切片沿用冻结的本次方案。
- `config.ini` 能保存长期默认值，交互选择只覆盖本次任务；非交互运行必须显式使用 `--yes`，并可通过命名预设或 `--route` 覆盖。
- 私密配置不进入 Git，日志中不出现完整密钥或 Cookie。
- 临时音频满 5 天后才可清理；清理不会删除用户成果。
- Windows Typora 能直接打开并保存 WSL 中的测试 Markdown；必要时可按需导出到 Windows。
- 幕布能导入学习总结的主要层级结构。
- 新电脑能依据文档恢复程序、用户成果和必要配置。

准确性采用组合评价：关键事实误写数、关键点遗漏数、全文覆盖率、时间戳可用率、术语识别率和人工可读性。具体阈值在阶段 2 用短样例校准后写入。

## 13. 当前结论与下一步准备

用户此前已确认：

1. 使用当前 ChatGPT 登录的 `codex exec` 处理文本。
2. 知识审校默认使用 `basic`。
3. 长期成果使用独立的 `/home/dev/bili-summary-data/`。
4. 接受第 6.1 节的规范命令格式：参数之间使用半角空格，链接和参数值统一放入英文双引号，尖括号占位符不实际输入。
5. 阶段 4 使用相同的 10 分钟片段比较阿里云 Qwen3 ASR Flash Filetrans 与 Paraformer v2；胜出模型作为无字幕默认在线后端，本地 CPU 结果保留为兜底参照，不再比较豆包。
6. 第一版支持单个本地 MP4；接受 Windows 或 WSL 路径，默认不复制原视频，使用 FFmpeg/ffprobe 处理媒体，不安装播放器。

本版已完成逐任务路由、DeepSeek 直接 HTTP 适配器、运行前选择、离线错误测试和首次 Flash/Pro 真实比较；`config.ini` 保存用户确认的长期默认方案：Flash high 整理、Codex high 总结、Pro high 审校。交互选择仍只覆盖本次任务。

用户已登录华北 2（北京）百炼并确认 Paraformer v2 剩余 36,000 秒。豆包已退出第一版比较范围，无需注册或保存第二家厂商凭据。

进入阶段 4 前按以下责任边界操作：

1. 工具侧已核对 Qwen3 Filetrans 与 Paraformer v2 的正式 API、北京地域、接口版本、公开额度和价格；用户已在登录控制台确认两项额度，实际用量仍以控制台为准。
2. 用户亲自完成 API Key 创建和计费确认；账号密码、短信验证码和二次验证始终由用户掌握。
3. 当前北京业务空间创建独立于其他用途的项目 API Key；第一版是个人单项目，不为权限隔离提前引入复杂的企业子账号体系。
4. API Key、App Key、Access Token 绝不发到聊天或写入本文档；用户只在本机秘密配置中填写。配置文件被 Git 忽略，密钥另存密码管理器以便迁移。
5. 先使用试用或免费额度。任何“转正式版、充值、购买资源包、启用自动充值”都不属于计划确认的授权，必须再次由用户明确决定。
6. 豆包和腾讯云都退出当前比较范围，不创建账号或配置。

阶段 4A 的真实音频样本提取与缓存复用、阶段 4B 的本地 CPU 基线和两个阿里云模型真实对比、阶段 4C 的完整本地 MP4 流程均已完成并验收。Qwen3 是默认在线后端，Paraformer 为在线回退，本地 CPU 仅在显式配置后启用。当前进入阶段 5，优先实现缓存占用、过期清单和安全清理，再进行极长视频及恢复演练。

2026-08-31 阶段 4B 的核对与实测记录在 `docs/stage4-transcription-evaluation.md`：本次明确使用 `qwen3-asr-flash-filetrans`，原价 0.00022 元/秒；Paraformer v2 原价 0.00008 元/秒。两项在线任务均成功，厂商返回用量下的公开原价估算合计约 0.14790 元；实际免费额度或账单扣减不作为项目验收依据。

### 13.1 当前执行环境基线

2026-08-30 的只读检查结果：

| 项目 | 当前状态 | 对实施的影响 |
|---|---|---|
| 系统 | WSL2，Linux 6.6，x86_64 | 与既定 Ubuntu 22.04 开发路径一致 |
| CPU | AMD Ryzen 7 7735H，8 核 16 线程 | 可以进行本地 CPU 转写，但速度仍需用阶段 4 的相同片段实测，不能只按核心数估计 |
| WSL 内存 | 7.4 GiB，可用约 5.6 GiB；Swap 2 GiB | 第一版本地转写需优先考虑内存可承受的模型，不提前下载大模型 |
| 磁盘 | 项目所在 WSL 文件系统可用约 952 GiB | 当前空间充足；5 天缓存策略仍保留，防止长期无界增长 |
| Python | 系统 Python 3.10.12 | Ubuntu 22.04 的默认 Python 系列；阶段 1 使用项目隔离环境，不修改系统 Python |
| FFmpeg | 已发现 `/usr/bin/ffmpeg` 与 `/usr/bin/ffprobe`，版本 4.4.2 | 阶段 4A 已用于只读探测；只在无字幕时生成临时转写样本，不依赖播放器或 Python 视频库 |
| GPU 接口 | WSL 内 `nvidia-smi` 无法初始化 NVML，未确认可用的 NVIDIA 计算接口 | 第一版不依赖 GPU；阶段 4 按 CPU 基线测试，不为未知显卡状态提前配置 CUDA 或 ROCm |
| Git | 当前分支为 `main`；阶段 1～3.1 已按阶段形成独立提交，阶段 4 的媒体、转写、内容模式、测试和文档共同形成下一稳定基线 | 各阶段基线可独立定位；用户成果和秘密配置始终不进入 Git |

这些是环境事实，不是永久配置。迁移电脑或调整 WSL 资源后，应重新运行只读检查；项目文档不把当前内存、磁盘或设备状态当作固定依赖。

## 14. 决策记录

| 编号 | 日期 | 决定 | 原因与影响 |
|---|---|---|---|
| D-001 | 2026-08-30 | 计划审阅完成前不实施 | 避免过早选型、安装和环境混乱 |
| D-002 | 2026-08-30 | 面向本机个人课程学习 | 暂不引入账号、远程服务和并发系统 |
| D-003 | 2026-08-30 | 第一版使用命令行 | 依赖少、便于逐步验证；UI 后置 |
| D-004 | 2026-08-30 | 文本处理采用云端模型，本地语言模型暂缓 | 当前尚未配置本地语言模型；具体访问路径在 v0.3 调整为 Codex CLI |
| D-005 | 2026-08-30 | 按主题组织用户成果 | 主要覆盖计算机、数学、电磁学和电子 |
| D-006 | 2026-08-30 | 临时音频统一保留 5 天 | 兼顾失败恢复与磁盘控制 |
| D-007 | 2026-08-30 | 输出标准 Markdown | 当前供 Typora、幕布使用，并给 Obsidian 留接口 |
| D-008 | 2026-08-30 | PPT 作为独立后续工具 | 先稳定内容结构，再处理演示设计与文件生成 |
| D-009 | 2026-08-30 | 画面提取后置 | 第一版聚焦字幕、转写和文本学习资料 |
| D-010 | 2026-08-30 | 链接测试集为三个主样例加一个边界样例 | 覆盖短、长、无字幕和接近 4 小时情况；v1.1 另以 D-023 增加一个本地 MP4 样例 |
| D-011 | 2026-08-30 | Windows Typora 不在 WSL 重复安装 | 已确认安装于 `D:\Programs\Typora`，标准 Markdown 可跨环境读取 |
| D-012 | 2026-08-30 | 用户成果保存在 WSL | Windows 侧只负责 Typora、幕布和 PPT 等展示或编辑 |
| D-013 | 2026-08-30 | 固定交付四份内容文件和一份任务记录 | 字幕、完整整理稿、审校报告、学习总结符合预期 |
| D-014 | 2026-08-30 | 知识风险只标记疑点，不自动联网查证 | 用户自行回看；未来独立建设高算力学习审核 Agent |
| D-015 | 2026-08-30 | 可逐次接受 OpenAI 转写价格超出 1 元/小时 | 当前没有独立 API 支付路径，因此仅保留未来候选 |
| D-016 | 2026-08-30 | ChatGPT Plus 与 Platform API 分开处理 | Plus 不作为 API 余额；ChatGPT 登录和 API Key 是不同的认证与计费路径 |
| D-017 | 2026-08-30 | 初始已验证文本后端使用当前 ChatGPT 登录的 `codex exec` | 用户已确认并完成阶段 2、3 实测；多模型改造后仍作为默认回归基线 |
| D-018 | 2026-08-30 | 知识审校默认使用 `basic` | 用户已确认；复用整理过程控制额度，`deep` 仍可按任务启用 |
| D-019 | 2026-08-30 | 长期成果默认放在 `/home/dev/bili-summary-data/` | 用户已确认；与源代码分离以降低误删和迁移遗漏风险 |
| D-020 | 2026-08-30 | 国内在线转写进入第一版实测范围 | 已确认豆包、阿里云和腾讯云存在适用 API；先小样本比较豆包与 Paraformer，只实现胜出者，本地保留为兜底 |
| D-021 | 2026-08-30 | 命令行采用带引号的规范格式 | 用户已确认；半角空格分隔参数，链接和参数值统一加英文双引号 |
| D-022 | 2026-08-30 | 转写账号推迟到阶段 4 前办理 | 阶段 1～3 不需要 API；避免试用期、密钥和计费状态过早引入，腾讯云暂不注册 |
| D-023 | 2026-08-30 | 第一版增加单个本地 MP4 输入 | 复用字幕、转写、审校和总结主流程；暂不批量扫描目录或扩展其他容器 |
| D-024 | 2026-08-30 | 本地媒体只新增 FFmpeg 工具依赖 | `ffmpeg` 与 `ffprobe` 覆盖轨道探测和音频提取；播放器与额外 Python 视频库不属于运行依赖 |
| D-025 | 2026-08-30 | 本地原视频默认原地读取 | 避免成果库被大文件撑大；用路径、元数据和 SHA-256 追踪，原视频由用户单独备份 |
| D-026 | 2026-08-30 | 文本处理新增逐任务命名配置和模型路由 | 整理、总结、Basic 与 Deep 审校可以独立选择后端、模型和推理档位；默认保留 Codex 行为 |
| D-027 | 2026-08-30 | DeepSeek 采用项目直接 HTTP 适配器 | 官方协议明确，能完整记录模型与用量；用 Python 标准库实现，不为 OpenAI 兼容格式额外安装 SDK |
| D-028 | 2026-08-30 | 不引入 ZCode，DeepSeek 由项目直接调用 | 官方 API 已满足模型与推理强度选择；减少中间层、运行时和升级耦合 |
| D-029 | 2026-08-30 | DeepSeek 文本调用不设费用阻断门槛，但保留用量和费用估算 | 用户已说明费用无需担心；记录仍用于归因、比较、异常检测和迁移审计 |
| D-030 | 2026-08-30 | 取得字幕后、首次文本模型调用前进行一次性运行方案确认 | 配置文件保存长期默认值，交互负责本次覆盖，显式参数兼顾以后无人值守自动化 |
| D-031 | 2026-08-30 | 首次 DeepSeek 对比后暂不替换 Codex 长期默认 | Flash/Pro 已有同条件证据，但 Codex 尚未使用同一套四任务提示词平行测试；保留基线避免把单样本结论扩大为全局决定 |
| D-032 | 2026-08-30 | 默认采用 Flash high 整理、Codex high 总结、Pro high 审校 | 用户完成 Codex 对比后认为其总结质量更合适；Flash 负责全文整理，Pro 审校聚焦知识与逻辑，过滤普通口语和文风猜测 |
| D-033 | 2026-08-31 | 在线转写默认采用 Qwen3 Filetrans，Paraformer v2 保留为备用 | 用户不再比较豆包；同片段工具抽查和人工审阅后确认 Qwen3 整体准确率更合适。两者均有免费额度，本地 CPU 继续兜底 |

## 15. 主要风险

| 风险 | 影响 | 应对 |
|---|---|---|
| 平台页面或接口变化 | 字幕、元数据或音频获取失败 | 隔离平台模块、保留真实样例、明确升级路径 |
| AI 字幕或转写错误 | 整理稿和总结误读 | 保留原字幕、独立文本审校、所有疑点附时间戳 |
| 讲者本身存在知识错误 | 学习资料传播错误 | 独立风险报告，不静默修正原意，用户回看确认 |
| Codex 订阅动态限流 | 长视频处理中断 | 切片落盘、断点恢复、避免重复调用，并保留其他模型适配器 |
| DeepSeek 限流、欠费或服务过载 | 长视频处理中断 | 标准化错误、指数退避、保存成功切片；欠费不自动充值，恢复后续跑 |
| DeepSeek 返回空内容、截断或不符合 Schema | 成果缺段或结构损坏 | 本地严格验证、限定重试、拒绝写入不完整最终成果 |
| 深度审校消耗过大 | 处理时间长、占用订阅或 API 用量 | 默认 `basic`，`deep` 按任务启用并提前展示模型、推理档位和规模 |
| 模型生成原文没有的结论 | 总结失真 | 重要结论映射到字幕位置；无依据则标记不确定 |
| 模型或推理档位变化后误用旧缓存 | 结果来源与配置不一致 | 缓存指纹包含任务、后端、厂商、模型、推理档位、提示词、Schema 和输入哈希 |
| Cookie 或 API 密钥泄露 | 账号与费用风险 | 不进入仓库、不打印日志、最小权限、提供撤销说明 |
| Codex 登录令牌泄露 | ChatGPT/Codex 账号风险 | 不复制或备份 `~/.codex/auth.json`，子进程只复用本机登录状态 |
| 保存模型原始思考内容 | 敏感信息和缓存体积增长 | 默认丢弃 `reasoning_content`，只记录推理 token 与最终结构化输出 |
| 在线转写超预算或出现欠费 | 产生意外账单、任务中断 | 调用前估算并设置程序侧提交门槛，关闭自动充值，配置厂商预算提醒，保存厂商返回用量与公开价格估算 |
| 在线转写需要上传音频 | 课程音频和临时 URL 离开本机 | 仅处理获授权内容，明确提示厂商；使用短期私有链接，取回结果后删除本项目创建的云端临时对象 |
| 同时接入多个语音转写厂商 | 依赖和配置膨胀、迁移困难 | 同一短样本比较后只保留一个正式在线转写适配器；密钥只存密码管理器 |
| 本地原视频移动或删除 | 无法重新提取字幕、音频或复现处理 | `source.json` 保存路径和哈希；已有成果保持可读，原视频由用户视频库单独备份 |
| 直接读取 `/mnt/d` 性能不足 | 媒体探测或提取音频变慢 | 先实测顺序读取；必要时使用受 5 天清理规则管理的 WSL 临时副本，不永久复制 |
| 缓存持续增长 | 磁盘耗尽 | 字幕优先、只取音频、稳定去重、5 天过期清理 |
| 工具更新破坏旧成果 | 无法复现或迁移 | 数据格式版本、变更前备份、恢复演练和兼容策略 |
| WSL 与 Windows 出现两个成果副本 | 修改冲突、备份混乱 | WSL 是唯一主数据；Windows 只按需导出展示副本 |
| 内容高度依赖画面 | 纯文本资料不完整 | 第一版明确提示局限，后续按需求增加关键帧分析 |

## 16. 合规和隐私边界

- 只处理用户有权访问并用于合法个人学习的内容。
- 本地 MP4 仅处理用户自行提供且有权使用的文件；工具不改变原文件，也不把原视频纳入成果备份。
- 尊重平台规则、访问控制和版权，不实现绕过限制的功能。
- 最终资料以归纳和学习整理为目标，不大段对外复制字幕或替代原作品。
- 调用 Codex 或 DeepSeek 前明确字幕会离开本机，并记录实际后端、厂商、模型、推理档位和用量；调用国内在线转写前明确音频会离开本机，并记录厂商、模型、用量和费用。本地转写时音频不离开本机。
- 不把 ChatGPT/Codex 登录令牌复制到项目、成果目录、日志或迁移备份。
- 不把 DeepSeek 或国内转写 API Key、临时上传签名 URL、完整云端对象地址写进成果、日志或 Git；迁移时只从密码管理器重新配置密钥。
- 若未来使用账号 Cookie，先说明存放位置、泄露风险、清除方式和失效处理。
